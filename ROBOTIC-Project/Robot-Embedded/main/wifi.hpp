#pragma once

/**
 * @brief Start the wifi and start the json server
*/
void activate_wifi_and_start_json_server(const int gpioLeftMotor1, const int gpioLeftMotor2, const int gpioRightMotor1, const int gpioRightMotor2);
